{
  "package": {
    "name": "easyconfig-parent",
    "repo": "maven",
    "subject": "jubicoy"
  },

  "version": {
    "name": "${TRAVIS_TAG}",
    "desc": "Tagged build",
    "released": "${RELEASED_DATE}",
    "vcs_tag": "${TRAVIS_TAG}",
    "attributes": [],
    "gpgSign": false
  },

  "files": [
    {
      "includePattern": "pom.xml",
      "uploadPattern": "/fi/jubic/easyconfig-parent/${TRAVIS_TAG}/easyconfig-parent-${TRAVIS_TAG}.pom"
    }
  ],
  "publish": true
}
